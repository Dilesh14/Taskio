<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Taskio.Views.SwipableView"
             xmlns:cards="clr-namespace:PanCardView;assembly=PanCardView"
             xmlns:controls="clr-namespace:PanCardView.Controls;assembly=PanCardView"
             >
    <ContentPage.Content>
        <StackLayout>
            <cards:CardsView ItemsSource="{Binding ItemSource}" x:Name="SwipeContainer" >
                <cards:CardsView.ItemTemplate>
                    <DataTemplate>
                        <AbsoluteLayout>
                            <Frame>
                                <Label  Text="{Binding Name}" WidthRequest="300" HorizontalOptions="Center" HeightRequest="200"/>
                            </Frame>
                            <Image x:Name="Picture"/>
                        </AbsoluteLayout>
                    </DataTemplate>
                </cards:CardsView.ItemTemplate>
            </cards:CardsView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>